{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug tau (fichier courant)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/tau",
            "args": ["${file}"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cmake: build"
        }
    ]
}
