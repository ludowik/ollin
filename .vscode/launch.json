{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Ollin: main.ol",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/ollin",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cmake: build",
            "terminal": "integrated",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}