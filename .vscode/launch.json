{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug ollin (test.ollin)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/ollin",
            "args": ["${workspaceFolder}/test.ollin"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cmake: build",
            "terminal": "integrated",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}
